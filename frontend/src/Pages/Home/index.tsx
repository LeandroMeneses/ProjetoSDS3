const Home = () => { // a função pode ser declarada assim também
    //function NavBar() { ou assim
    return (
        <div>
            <h1>Página Home</h1>
        </div>
    );
}

export default Home;